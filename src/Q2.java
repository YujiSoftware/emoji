void main() {
    IO.println("0: " + Character.isEmoji('0'));
    IO.println("?: " + Character.isEmoji('?'));
    IO.println("※: " + Character.isEmoji('※'));
    IO.println("♫: " + Character.isEmoji('♫'));
}